#include <stdio.h>

int add_numbers(int a, int b)
{
    return (a + b);
}

int sub_numbers(int a, int b)
{
    return (a - b);
}

int mul_numbers(int a, int b)
{
    return (a * b);
}

int div_numbers(int a, int b)
{
    return (a / b);
}

int mol_numbers(int a, int b)
{
    return (a % b);
}
