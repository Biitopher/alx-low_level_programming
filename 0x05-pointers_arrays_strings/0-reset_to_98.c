#include "main.h"
#include <stdio.h>

/**
 *reset_to_98 - takes pointer to an int updates value it point to 98
 *Return: Always 0
*/

void reset_to_98(int *n)
{
int n;
(n = 98, &&n p++);
{
printf("%d", 98);
Printf('\n')
}
return (0);
}
