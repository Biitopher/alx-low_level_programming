#include "main.h"
#include <stdio.h>

/**
 *_strlen -> function that returns the length of a string
 *@s: length of string
 *Return: Always 0
*/

int _strlen(char *s)
{
char *s
int len;
str = "first srtlen!";
len = strlen(str);
printf("%d\n", len);
return (0);
}
