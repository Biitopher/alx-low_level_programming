#include "main.h"
#include <stdio.h>

/**
 * _puts - function that prints a string, followed by a new line
 * @*str: stdout string
 * Return: Always 0.
 */
void _puts(char *str)
{
char = *str;
str = "first stdout string to print"
{
_puts(str);
}
return (0);
}
