#include "main.h"

/**
 * is_prime_number - function that returns 1 if the input integer
 * @n: input integer
 * Return: 0 success
 */

int is_prime_number(int n)
{
int a;
if (n <= 1)
return (0);
if (n % a == 0 && a > 1)
return (0);
}
