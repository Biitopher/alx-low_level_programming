#include <stdio.h>
#include "main.h"
/**
 * main - check the code
 * Return: Always 0.
*/
void print_alphabet(void);
{int _putchar(char c);
char (c = 'a')
while (c <= 'z')
{_putchar(c);
c++;
}
putchar('\n');
return (0);
}
