#include <stdio.h>
#include "main.h"
/**
 * Checks for lowercase characters
 * Return: returns 1 and 0 depending on condition
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
putchar('\n');
}
