#include <stdio.h>
#include "main.h"
/**
 * _islower(c) -> Checks for lowercase characters
 * Return: returns 1 and 0 depending on condition
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
