#include "main.h"
#include<stdio.h>

/**
 * main - Entry point
 * @*str: string
 * Return: Always 0 (Success)
 */

void _puts(char *str)
{
for
(; *str != '\0'; str++)
while (*str != '\0')
{
putchar(*str);
putchar(*str++);
}
putchar('\n');
}
